# Flask Minimal Application with Docker

Flask Application with Docker and Gunicorn WSGI server.
It is here just in case if i need it somewhere :)

## Executing App
```
docker-compose up -d --build
```

## TODO
Flask logging with Gunicorn